[Unit]
Description=Pods MCP Server
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/pods
ExecStart=VENV_PLACEHOLDER POD_PLACEHOLDER server start --tui
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
